spring:
  profiles:
    active: dev

eureka:
    instance:
        prefer-ip-address: false
    client:
        serviceUrl:
            defaultZone: http://admin:123456@localhost:9090/eureka
        register-with-eureka: false

---
spring:
  profiles: dev
  application:
    name: home-web
  cloud:
     config:
       label: master
       profile: dev
       discovery:
        enabled: true
        service-id: config-server

---
spring:
  profiles: online
  application:
    name: home-web
  cloud:
     config:
       label: master
       profile: online
       discovery:
        enabled: true
        service-id: config-server
